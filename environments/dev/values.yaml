namespace: agent-dev
replicaCount: 1

image:
  repository: agent-kyc
  tag: local
  pullPolicy: Never

service:
  type: ClusterIP
  port: 80
  targetPort: 8000

env:
  ENV_NAME: dev
  LOG_LEVEL: debug
  OTEL_SERVICE_NAME: agent-kyc-dev
  OTEL_EXPORTER_OTLP_ENDPOINT: ""

secrets:
  MODEL_API_KEY: dummy-local-key
  DB_PASSWORD: dummy-local-password